<h1> Inventory </h1>

{% if latest_inventory_list %}
    <ul>
    {% for inventory in latest_inventory_list %}
        <li>{{ inventory.material }}: {{ inventory.stock_material }}</li>
    {% endfor %}
    </ul>
{% else %}
    <p>No items in inventory.</p>
{% endif %}

<a href="{% url 'index' %}">Return back to Main Menu</a>
